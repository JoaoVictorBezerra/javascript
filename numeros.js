// Tipo number
const meuNumero = 7;
const firstNumber = 2
const secondNumber = 3;

const opMatematica = firstNumber + secondNumber;

console.log(opMatematica);

// Ponto flutuante.

const _float = 3.5;
console.log(_float * 2);

// NaN -> Not a Number (não é um numero)

const alura = "Alura";
console.log(alura + firstNumber);

var a = 10
var b = 0
console.log(a/b) // Infinity